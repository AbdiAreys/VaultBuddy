import sqlite3

conn = sqlite3.connect("vaultbuddy.db")
c = conn.cursor()

# List all tables
c.execute("SELECT name FROM sqlite_master WHERE type='table';")
print("Tables:", c.fetchall())

# List all rows in secrets table
c.execute("SELECT * FROM secrets;")
print("Secrets:", c.fetchall())

conn.close()
